#pragma once //not exactly sure what this does, but as i've read its an include guard, kinda like a restriction to make you fail less later on from what i understand

//int winningAnswer; //im not sure what's a good way to transition data between files so i did this.

int generateAnswer();